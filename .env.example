# ══════════════════════════════════════════════════════════════
# WEABOO API — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ══════════════════════════════════════════════════════════════

# ── Server ─────────────────────────────────────────────────────
PORT=3000

# ── Supabase ───────────────────────────────────────────────────
# Found in: Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ── Cloudflare Workers (video stream proxy) ────────────────────
# URL of your deployed Cloudflare Worker.
# After deploying cloudflare-worker/worker.js:
#   wrangler secret put SUPABASE_URL
#   wrangler secret put SUPABASE_SERVICE_ROLE_KEY
#   wrangler deploy
# Then set this to your worker URL:
CLOUDFLARE_WORKERS_URL=https://weaboo-stream.your-subdomain.workers.dev

# ── HuggingFace Space (background worker) ─────────────────────
# URL of the FastAPI app running in your HuggingFace Space.
# After pushing huggingface-space/ to your HF Space repo,
# the app will be live at: https://{username}-{space-name}.hf.space
HF_SPACE_WEBHOOK_URL=https://your-username-weaboo-worker.hf.space

# ── Weaboo API URL (untuk cache invalidation dari HF Space) ───
# URL publik Weaboo API kamu — dipakai oleh HF Space untuk invalidate
# streaming cache setelah video selesai di-upload ke HuggingFace.
# Contoh: https://weaboo-api.railway.app atau http://your-server-ip:3000
WEABOO_API_URL=https://your-weaboo-api-url.com

# ── File obfuscation salt ──────────────────────────────────────
# Secret salt used to generate obfuscated video filenames (SHA-256).
# Must be the same value in both Weaboo API and HuggingFace Space env vars.
# Generate with: openssl rand -hex 32
HF_FILE_SALT=change-this-to-a-random-secret-string
